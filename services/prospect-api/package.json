{
  "name": "prospect-pipeline-backend",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json && npm run build:docs",
    "build:docs": "cd docs-site && npm run build",
    "start": "node dist/index.js",
    "test": "echo \"No tests yet\" && exit 0",
    "docs:dev": "cd docs-site && npm start",
    "docs:build": "cd docs-site && npm run build"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.922.0",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.19.2",
    "firebase-admin": "^12.6.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.9.0",
    "@types/uuid": "^10.0.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
